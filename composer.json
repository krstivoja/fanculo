{
    "name": "fanculo/wordpress-plugin",
    "description": "Simple WordPress plugin with React settings",
    "type": "wordpress-plugin",
    "require": {
        "php": ">=7.4"
    },
    "autoload": {
        "psr-4": {
            "Fanculo\\": "app/"
        }
    },
    "config": {
        "optimize-autoloader": true
    }
}