import './main.css';

console.log('Fanculo app loaded!');

document.addEventListener('DOMContentLoaded', function() {
    const app = document.getElementById('app');
    if (app) {
        app.innerHTML = '<h2>🚀 CORS Fixed! 🚀</h2><p>Your Vite setup is working correctly with hot reload!aaaasasda</p>';
    }
});